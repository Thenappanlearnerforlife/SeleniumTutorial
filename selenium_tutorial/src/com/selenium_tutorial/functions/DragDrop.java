package com.selenium_tutorial.functions;

import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.interactions.Actions;
import org.testng.annotations.Test;

public class DragDrop {

	/*
	 * public static void main(String[] args) throws InterruptedException {
	 * System.setProperty("webdriver.chrome.driver",
	 * "C:\\Users\\Thenappan Saravanan\\Downloads\\Selenium Jars\\ChromeDriver\\chromedriver.exe"
	 * ); WebDriver driver = new ChromeDriver();
	 * driver.manage().window().maximize();
	 * 
	 * driver.get("https://demoqa.com/selectable/"); Thread.sleep(2000); Actions
	 * actions = new Actions(driver); // actions.dragAndDrop(driver.findElement(By.
	 * xpath("//*[text()='Drag me to my target']")), //
	 * driver.findElement(By.xpath("//*[@id='droppable']"))).build().perform();
	 * actions.click(driver.findElement(By.xpath("//*[text()='Item 1']"))).keyDown(
	 * Keys.CONTROL)
	 * .click(driver.findElement(By.xpath("//*[text()='Item 2']"))).build().perform(
	 * ); // actions.wa }
	 */
	
	@org.testng.annotations.BeforeSuite
	public void BeforeSuite
	() {
		
	}
	
	@Test
	public void TestCase1() {
		
	}
	
	
	@Test
	public void TestCase2() {
		
	}
}
